(function initEditingCore(globalScope) {
  const root = globalScope || window;
  root.NexaMapCore = root.NexaMapCore || {};

  // Phase 2 scaffold: edit session and undo/redo helpers will move here.
  root.NexaMapCore.editing = root.NexaMapCore.editing || {};
})(window);
