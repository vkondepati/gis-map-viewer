(function initAnalysisCore(globalScope) {
  const root = globalScope || window;
  root.NexaMapCore = root.NexaMapCore || {};

  // Phase 2 scaffold: buffer and spatial analysis helpers will move here.
  root.NexaMapCore.analysis = root.NexaMapCore.analysis || {};
})(window);
