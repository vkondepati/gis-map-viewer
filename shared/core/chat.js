(function initChatCore(globalScope) {
  const root = globalScope || window;
  root.NexaMapCore = root.NexaMapCore || {};

  // Phase 2 scaffold: map assistant payload and UI helpers will move here.
  root.NexaMapCore.chat = root.NexaMapCore.chat || {};
})(window);
