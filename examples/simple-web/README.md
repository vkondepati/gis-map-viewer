# NexaMap Simple Example

This example demonstrates a tiny `GISMap` wrapper (using Leaflet) and exercises basic API calls extracted from the docs: `fitBounds`, `setZoom`, `panTo`, `flyTo`, and `reset`.

Open `examples/simple-web/index.html` in a browser (or serve the folder) to try it.
